package com.britetodo.turbotrack.theme

import androidx.compose.ui.graphics.Color

// ── Brand ──────────────────────────────────────────────────────────────────
val TurboBlue       = Color(0xFF007AFF)   // iOS system blue
val TurboBackground = Color(0xFFF2F2F7)   // iOS systemGroupedBackground
val TurboCard       = Color(0xFFFFFFFF)   // white cards
val TurboCardAlt    = Color(0xFFF2F2F7)   // tertiary grouped bg
val TurboDivider    = Color(0xFFC6C6C8)   // iOS separator

// ── Text ───────────────────────────────────────────────────────────────────
val TextPrimary   = Color(0xFF1C1C1E)   // iOS label
val TextSecondary = Color(0xFF6E6E73)   // iOS secondary label
val TextMuted     = Color(0xFFAEAEB2)   // iOS tertiary label
val TextBlue      = Color(0xFF007AFF)

// ── Dark navy (used in Onboarding dark screens) ────────────────────────────
val TurboNavy      = Color(0xFF0A0E1A)   // dark background
val TurboNavyMid   = Color(0xFF141929)   // mid-dark surface
val TurboNavyLight = Color(0xFF1E2640)   // lighter dark surface
val OrbBlue        = Color(0xFF007AFF)   // orb gradient blue
val OrbCyan        = Color(0xFF00C7FF)   // orb gradient cyan
val OrbPurple      = Color(0xFF5E5CE6)   // orb gradient purple
val ChipBlue       = Color(0xFF007AFF)   // selected chip blue

// ── Severity palette ───────────────────────────────────────────────────────
val SeverityNone     = Color(0xFF34C759)  // green
val SeverityLight    = Color(0xFFFFCC00)  // yellow
val SeverityModerate = Color(0xFFFF9500)  // orange
val SeveritySevere   = Color(0xFFFF3B30)  // red
val SeverityExtreme  = Color(0xFFC0392B)  // dark red
